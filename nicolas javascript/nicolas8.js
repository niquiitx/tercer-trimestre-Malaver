// Solicita al usuario un número límite y lo convierte a entero
let david67 = parseInt(prompt("Ingrese un número para contar hasta él nicolas ruiz:"));
// Verifica si el límite es un número válido y positivo
if (isNaN(limite) || limite <= 0) {
alert("Por favor, ingrese un número entero positivo válido.");
} else {
let nicolas_ruiz68 = "Contando:\n"; // Inicia un mensaje para acumular los números
// Bucle 'for' que itera desde 1 hasta el número límite
for (let i = 1; i <= limite; i++) {
mensaje += i + "\n"; // Añade cada número al mensaje
}
alert(mensaje); // Muestra el mensaje final con todos los números
}