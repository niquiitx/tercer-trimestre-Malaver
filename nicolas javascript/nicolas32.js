// Versión mejorada con validación
let david25 = prompt("Ingrese primer valor nicolas ruiz:");
let nicolas_ruiz26 = prompt("Ingrese segundo valor nicolas ruiz:");

// Conversión con base decimal
let n1 = parseInt(valor1, 10);
let n2 = parseInt(valor2, 10);

// Verificar conversión exitosa
if (isNaN(n1) || isNaN(n2)) {
alert("Por favor ingrese números válidos nicolas ruiz");
} else {
let nicolas_ruiz6 = n1 + n2;
alert(`El resultado de ${n1} + ${n2} = ${resultado}`);
}