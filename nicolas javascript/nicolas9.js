// Solicita al usuario un número y lo convierte a entero
let david69 = parseInt(prompt("Ingrese un número para verificar si es par o impar nicolas ruiz:"));
// Verifica si el número ingresado es un número válido
if (isNaN(numero)) {
alert("Por favor, ingrese un número entero válido.");
} else {
// Utiliza el operador módulo (%) para verificar si el resto de la división por 2 es 0
if (numero % 2 === 0) {
alert("El número " + numero + " es par."); // Mensaje para números pares
} else {
alert("El número " + numero + " es impar."); // Mensaje para números impares
}
}